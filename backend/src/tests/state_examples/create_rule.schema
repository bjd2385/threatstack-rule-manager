{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "state",
  "type": "object",
  "properties": {
    "organizations": {
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z0-9]{24}$",
        "type": "object",
        "propertyNames": {
          "pattern": "^[a-z0-9-]{36}$",
          "type": "object",
          "propertyNames": {
            "pattern": "^[a-z0-9-]{36}$",

          }

        }
      },
      "minProperties": 0
    },
    "workspace": {
      "type": "string"
    }
  },
  "required": [
    "organizations",
    "workspace"
  ]
}